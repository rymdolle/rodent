%% -*- erlang -*-
[{kernel, [{logger, [{handler, default, logger_std_h,
                      #{level => notice,
                        formatter => {logger_formatter, #{depth => 30}},
                        config => #{type => standard_io}}}]}]},
 {rodent, [{host, "gopher.rymdis.com"},
           {port, 70},
           {routes, [#{path => <<"/">>,
                       callback => rodent_static,
                       options => {priv_file, rodent, "index.txt"}}]}]}].
