%% -*- erlang -*-
[{rodent, [{host, "localhost"},
           {port, 7070},
           {routes, [#{selector => <<"/*">>,
                       callback => rodent_static,
                       args => {format, {priv_file, rodent, "index.txt"}}},
                     #{selector => <<"/test/*">>,
                       callback => rodent_static,
                       args => {priv_dir, rodent, "foo"}}]}]}].
